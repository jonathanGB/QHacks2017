<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :bugs, class: "control-label" %>
    <%= number_input f, :bugs, class: "form-control" %>
    <%= error_tag f, :bugs %>
  </div>

  <div class="form-group">
    <%= label f, :shirtColor, class: "control-label" %>
    <%= text_input f, :shirtColor, class: "form-control" %>
    <%= error_tag f, :shirtColor %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
